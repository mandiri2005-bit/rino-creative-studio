.env
# ANCHORED to repo root only (leading /). Unanchored `data/` also matched python/data/ (the Nusantara
# corpus, COPY'd by python/Dockerfile) and made Railway's fresh build-snapshot EXCLUDE it → `COPY data/`
# failed ("/data not found"). python/data/ is tracked + needed in the image, so ignore ONLY ./data.
/data/
mcp-folder/
node_modules/
__pycache__/
*.pyc
.mcp_embed_cache.pkl
.env.local
.env.*.local
.env.development
.env.staging
.env.production

GOOGLE API KEY.txt
client_secret.json

# === cleanup additions ===
.DS_Store
**/.DS_Store
*.bak
*.bak2
env prod
env2.example
*copy*.yml
*copy*.html
nginx.text
spa.test
node_modules/
__pycache__/
*.pyc
.env
.env.local
# eval run artifacts — keep the dir, ignore its contents
eval/results/*
!eval/results/.gitkeep

# local 16:9 safety backups before 9:16 work (never push)
_wb_backup/
